id: "path-traversal-fix-001"
title: "路径遍历漏洞修复"
type: "code_fix"
difficulty: "medium"
source: "training-gym"
languages: ["python"]
test_cases_path: "tests/test_path_traversal_001.py"
evaluation:
  metrics:
    - name: "functional_pass"
      weight: 1.0
      type: "pytest"
    - name: "security_pass"
      weight: 1.0
      type: "static_analysis"
      config:
        rules: ["no_path_traversal"]
tags: ["python","security","path-traversal","code-fix"]
reward_virtual: 75
description: |
  修复路径遍历漏洞。
  确保用户输入的文件路径不会逃逸到允许目录之外。
hints:
  - "使用 os.path.abspath 规范化路径"
  - "验证规范化后的路径是否在允许的基础目录下"
